[package]
name = "files-rusted"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
slint = "1.15.1"
jwalk = "0.8"

[build-dependencies]
slint-build = "1.15.1"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = true
